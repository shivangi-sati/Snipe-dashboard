{
  "name": "snipe-fullstack",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build-client": "cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "json-server": "^0.17.4"
  }
}
